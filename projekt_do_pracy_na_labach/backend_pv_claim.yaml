apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-backend-claim
  namespace: app-namespace
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
