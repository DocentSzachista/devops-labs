apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend
  labels:
    app: {{ .Chart.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: 8000
    targetPort: {{ .Values.service.port }}
  selector:
    app: {{ .Chart.Name }}
