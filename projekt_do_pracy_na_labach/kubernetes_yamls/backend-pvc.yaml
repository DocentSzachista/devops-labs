apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-volume-claim
  namespace: apka
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: standard