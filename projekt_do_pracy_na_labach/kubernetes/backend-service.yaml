apiVersion: v1
kind: Service
metadata:
  namespace: moj-namespace
  name: backend-service
spec:
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 80 
    targetPort: 8000  
  type: NodePort