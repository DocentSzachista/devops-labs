apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiszki.fullname" . }}
  labels:
    {{- include "fiszki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiszki.selectorLabels" . | nindent 4 }}
