apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiszki.fullname" . }}-test-connection"
  labels:
    {{- include "fiszki.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fiszki.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
